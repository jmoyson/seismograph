node_modules
dist
build
.env
*.log
.DS_Store
coverage
.turbo
generated

# Claude Code: track shared bits (settings.json, skills/, commands/, agents/)
# but ignore per-user state. settings.local.json holds the local tool
# permission allowlist; projects/ holds session cache.
.claude/settings.local.json
.claude/projects/
